import React from 'react';

const TextComponents = ({text, description}) => {
    return (
        
        <>
            <p>{text}</p>
            <h1>{description}</h1>
        </>
    );
};

export default TextComponents;
